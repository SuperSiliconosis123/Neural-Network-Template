#include <iostream>
#include <string>
using namspace std;

#define NODE-SIZE 2 //defines the size of every node.
#define LAYERS 3 //defines the number of layers between the input and output layers

#define INPUT-NODES 10  //defines the number of input nodes. note: nodes are NOT binary. they may have any number of values in the range of their data type
#define MIDDLE-NODES 30 //defines the number of middle nodes.
#define OUTPUT-NODES 10 //defines the number of middle nodes.

class net {
  public:
    class node {
      public:
      
    class wire {
      public:
        
    };
};
